/**
 * VerifyPluginDrawer — Plugin drawer router.
 */

import type { PluginDrawerCanvasProps } from '@tryvienna/sdk';
import { VerifyDrawer } from './VerifyDrawer';

export function VerifyPluginDrawer(_props: PluginDrawerCanvasProps) {
  return <VerifyDrawer />;
}
